aligned_bam_dir:
  class: Directory
  path: /home/ubuntu/kinnex_longreads/outputs/pbmm2_align_scatter_test/
flnc_bam_dir:
  class: Directory
  path: /home/ubuntu/kinnex_longreads/outputs/isoseq_refine_test/

# Optional parameters (uncomment to customize)
# aligned_pattern: '^mapped\\..*\\.bam$'  # Only override if needed
# flnc_pattern: '^flnc\\..*\\.bam$'       # Only override if needed
# min_aln_coverage: 0.99
# min_aln_identity: 0.95
# threads: 4
# log_level: INFO